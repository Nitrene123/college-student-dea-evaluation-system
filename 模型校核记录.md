# 智能 DEA 评价系统模型校核记录

## 1. 校核范围

本记录用于 V1.1 版本的功能验收，覆盖径向 DEA、标准 SBM、超效率 SBM 和 Malmquist 指数四条计算路径。校核目标是确认输入约束、线性规划求解、结果关系和跨期输出均能被程序稳定处理。

## 2. 校核结果

| 模块 | 数据 | 关键结果 | 状态 |
|---|---|---:|---|
| 径向 DEA（BCC） | `data/demo_dea.csv` | 12 个 DMU；7 个效率前沿 DMU；平均效率 0.992323 | 通过 |
| DEA 数值审计 | `data/demo_dea.csv` | 所有效率在 `[0,1]`；CCR ≤ BCC；规模效率 ≤ 1；行数一致 | 通过 |
| 标准 SBM | `data/hospital_public.csv` | 31 个 DMU；15 个标准 SBM 有效 DMU；平均效率 0.724827 | 通过 |
| 超效率 SBM | `data/hospital_public.csv` | 31 个 DMU；15 个标准 SBM 有效 DMU；平均可行超效率 0.466853 | 通过 |
| Malmquist | `data/economy_public.csv` | 28 个 DMU；5 个时期；平均 Malmquist 指数 0.926720 | 通过 |

## 3. 结果解释边界

- DEA 是样本内相对效率评价，效率值不能直接解释为绝对生产率或因果效应。
- 超效率 SBM 排除被评价 DMU 后，个别 DMU 可能不存在可行解。系统将其标记为“超效率 SBM（无可行解）”，不会静默删除或伪造分数。
- Malmquist 指数按相邻时期计算；当跨期前沿之间不存在共同可行解时，系统保留可计算记录并在表格中显示可用结果。
- 指标方向、单位和 DMU 口径仍需由使用者结合业务场景确认。

## 4. 复核方式

在项目目录执行：

```bash
python -m py_compile app.py dea_engine.py
```

并按 README 中的数据源和配置在网页端切换四种模型。结果表可导出为 CSV，配置可导出为 JSON，便于留痕和复现。
