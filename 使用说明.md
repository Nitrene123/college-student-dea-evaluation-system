# 智能大学生学业成绩因素评估系统 V1.2 使用说明

## 一、系统用途

本系统面向大学生课程学习评价，使用运筹学中的数据包络分析（DEA）。教师将学生或班级作为决策单元（DMU），把学习时长等资源作为投入，把出勤、作业完成、课堂参与和课程成绩等“越高越好”的结果作为产出，计算相对学业效率、参考标杆、规模报酬和改进目标，并支持标准 SBM、超效率 SBM 与 Malmquist 指数分析。

## 二、启动系统

在项目目录打开 PowerShell，执行：

```powershell
python -m pip install -r requirements.txt
streamlit run app.py
```

浏览器访问终端显示的地址，当前开发配置通常为 `http://localhost:8512/`。也可以双击 `启动系统.ps1` 启动。

## 三、最快上手流程

1. 左侧“数据与模型”中先选择“内置学生成绩演示数据”。
2. 选择分析模型，首次使用建议选择“径向 DEA（CCR / BCC）”。
3. 在“评价配置”中确认 DMU、投入指标和期望产出字段。
4. 选择规模报酬：BCC 适合允许不同规模报酬的场景，CCR 适合固定规模报酬假设。
5. 选择投入导向或产出导向。
6. 在右侧查看“结果总览”“效率排行”和“改进诊断”。
7. 在“数据与模型”中查看数值校核，并下载结果 CSV 或模型配置 JSON。

## 四、数据源

### 1. 内置大学生成绩演示数据

用于快速体验，包含学生ID、专业班级、学习时长、出勤率、作业完成率、课堂参与度和期末成绩等字段。

### 2. 上传学生成绩文件

推荐每行一名大学生，至少包含：

- 学号或学生ID；
- 专业班级（可选）；
- 学习时长等投入字段；
- 出勤率、作业完成率、课堂参与度、期末成绩或课程成绩等结果字段。

项目目录中的 `data/大学生成绩模板.csv` 可直接复制后替换为真实数据。

### 3. 公开医院效率数据

适合医院或医疗机构效率研究。默认建议：

- 投入：床位数、卫技人员数
- 期望产出：诊疗人次数、入院人数
- 非期望产出：医疗废弃物

该数据适合径向 DEA、SBM 和非期望产出分析。

### 4. 公开工业面板数据

这是“地区 × 年份”面板数据，同一地区在多个年份会重复出现。系统选择该数据源时会自动切换到 Malmquist 指数。

如果确实要做单期径向 DEA，需要先把数据筛选为一个时期，保证每个 DMU 只出现一次。

### 5. 通用数据文件

“上传学生成绩文件”也支持 CSV、TSV、Excel（`.xlsx`、`.xls`）和 Parquet。上传后，在侧边栏手动选择字段含义；如果上传的不是学生数据，仍可按通用 DEA 数据配置投入和产出。

## 五、大学生数据与模型口径

系统评估的是“学习资源投入 → 学习结果与课程成绩”的相对转化效率。例如，某学生学习时间较少但学习结果和课程成绩较高，可能在当前样本中表现为较高的相对学习效率；这不等于学习时间对成绩没有因果作用，也不能替代回归、实验或教育测量研究。

## 六、数据格式要求

径向 DEA、SBM 和超效率 SBM 的数据应满足：

- 每行对应一个唯一 DMU；至少 3 个 DMU。
- 投入和产出必须是数值型、非负数。
- 至少选择 1 个投入指标和 1 个产出指标。
- 同一指标不能同时作为投入和产出。
- 每个 DMU 的投入合计和产出合计不能为 0。

Malmquist 数据应满足：

- 至少包含 DMU 字段和时期字段。
- 同一 DMU 至少出现在两个时期。
- 各时期的投入、产出字段保持一致，且为非负数值。

示例表结构：

| DMU | Period | Capital | Labor | Output |
|---|---:|---:|---:|---:|
| A | 2020 | 100 | 20 | 80 |
| A | 2021 | 110 | 21 | 88 |
| B | 2020 | 120 | 25 | 90 |
| B | 2021 | 125 | 24 | 96 |

## 七、模型选择

### 径向 DEA（CCR / BCC）

输出 DEA 效率、CCR 效率、BCC 效率、规模效率、规模报酬、标杆数量和改进项数。

- 投入导向：在产出不变时，分析投入最多可以压缩多少。
- 产出导向：在投入不变时，分析产出可以扩张多少。
- BCC：可变规模报酬，适合规模差异明显的 DMU。
- CCR：固定规模报酬，适合固定规模假设。

### SBM 效率

直接利用投入冗余、期望产出不足和非期望产出超额计算非径向效率。适合希望同时分析多类松弛量的场景。

### 超效率 SBM

计算时排除被评价 DMU，可用于区分多个标准效率为 1 的 DMU。若排除自身后没有可行解，系统会显示“超效率 SBM（无可行解）”，不会用伪造分数代替。

### Malmquist 指数

用于面板数据的相邻时期比较，输出：

- EC：效率变化
- TC：技术进步
- Malmquist 指数：生产率变化

通常指数大于 1 表示生产率提升，小于 1 表示生产率下降，等于 1 附近表示基本不变。

## 八、结果页面说明

- “结果总览”：查看平均效率、有效单元数量、效率分布和结果表。
- “效率排行”：查看完整排序，并下载当前结果 CSV。
- “改进诊断”：选择一个 DMU，查看参考标杆、权重、投入压缩、产出提升、冗余和不足。
- “数据与模型”：查看数据预览、当前字段配置、DEA 数值校核，并下载配置 JSON。

## 九、常见问题

### 提示“DMU 名称存在重复”

径向 DEA、SBM 和超效率 SBM 要求每行是唯一 DMU。若数据是多年面板数据，请选择 Malmquist；若要做单期 DEA，请先筛选一个年份。

### 提示“投入和产出存在空值或非数值”

请清理空值、单位字符、逗号和文本注释。例如将 `1,234` 转成数值 `1234`。

### 平均效率很高是否代表绝对优秀

不一定。DEA 是样本内相对效率评价，效率值依赖 DMU 样本、指标选取、单位和方向设定，不能直接替代因果分析或行业标准。

### 为什么超效率 SBM 有些行没有分数

排除自身后，某些 DMU 可能不存在可行参考组合。系统保留该状态，便于研究者识别模型边界。

## 十、建议的教学与研究流程

先用内置大学生成绩数据检查操作流程，再导入教师实际课程数据；确定指标口径后分别运行 BCC、CCR 和 SBM；最后对重点学生或班级查看改进诊断，并把 CSV、JSON 和数据来源一起留档。正式教学评价、论文、专利或软著材料中应同时说明数据来源、指标定义、模型假设和结果解释边界。
